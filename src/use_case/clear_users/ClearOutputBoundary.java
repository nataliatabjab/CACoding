package use_case.clear_users;

// Complete me. DONE

public interface ClearOutputBoundary {
    void prepareFailView(ClearOutputData clearOutputData);
    void prepareSuccessView(ClearOutputData clearOutputData);
}
